 <img
      class="card-img-top"
      src= "{{image_url}}"
      alt=" {{name}}"
    />
    <div class="card-body">
      <h5><a href="/beers/{{id}}">{{name}}</a></h5>
      <p class= "card-tagline"> {{tagline}}</p>
      <p class="card-text">{{description}}</p>
    </div>